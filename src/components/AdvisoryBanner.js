// src/components/AdvisoryBanner.js
import React from 'react';
import './AdvisoryBanner.css';

const AdvisoryBanner = () => (
  <div className="advisory-banner">
    <p><strong>Advisory:</strong> We only operate services from the RANDOM.ORG domain. Other sites that claim to be operated by us are impostors. If in doubt, <a href="mailto:contact@random.org">contact us</a>.</p>
  </div>
);

export default AdvisoryBanner;
